import PostMessage from '../models/plantDetails.js';

export const getPlants = (req, res) => {
    res.send('THIS WORKS!');
}

export const createPlants = (req, res) => {
    res.send('Post Creation!');
}